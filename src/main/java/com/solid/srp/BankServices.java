package com.solid.srp;

/**
 * Single Responsibility Principle
 * The single responsibility principle states that every java class
 * must perform a single functionality. Implementation of multiple
 * functionalities in a single class mashup the code
 */
public class BankServices {
    public long deposit(long amount,String accNo)
    {
        //coming soon
        return Long.parseLong(null);
    }
    public long withDraw(long amount,String accNo)
    {
        //coming soon
        return Long.parseLong(null);
    }

}
